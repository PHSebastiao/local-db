# Which services start. Edit the list, then run: docker compose up -d
# Available: mongo, mysql, postgres, redis, mariadb, elasticsearch, versitygw, clickhouse
COMPOSE_PROFILES=mongo,mysql,postgres,redis
